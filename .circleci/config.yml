version: 2.1
jobs:
  build:
    machine: true
    parallelism: 10
    resource_class: logan/runner-provisioner-test
    steps:
      - run: echo IT WORKS!
workflows:
  build-workflow:
    jobs:
      - build
