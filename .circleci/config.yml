version: 2.1
jobs:
  build:
    machine: true
    resource_class: logan/machine-runner
    steps:
      - run: whoami
      - run: mkdir test-results
      - run: sudo apt-get update && sudo apt-get install speedtest-cli
      - run: sudo mkdir test-results
workflows:
  build-workflow:
    jobs:
      - build
